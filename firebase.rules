rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Trình duyệt không truy cập Firestore trực tiếp. Cloudflare Worker dùng IAM.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
